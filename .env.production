# Public canonical origin (not a secret) — used for metadataBase,
# robots, sitemap and JSON-LD. Committed on purpose so every build
# resolves the same production URL.
NEXT_PUBLIC_SITE_URL=https://gazportfolio.vercel.app
